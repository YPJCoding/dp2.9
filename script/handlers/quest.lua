-- 任务相关道具 handler 模板
--
-- 当前文件仅提供模块边界，暂不注册任何现有业务逻辑。

local M = {}

function M.register(item_handler, ctx)
    -- 后续迁移：
    -- - 主线任务清理券
    -- - 支线/普通任务清理券
    -- - 每日任务清理券
    -- - 成就任务清理券
end

return M
